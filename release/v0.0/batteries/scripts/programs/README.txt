python programs associated with commands

must:
	- have the same name as the command
	- have implemented a function with the same name as the command
	- accept dev object as an argument (even if not used)
